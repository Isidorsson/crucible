// Globals (Go class, crucible API) live in src/app.d.ts so they are
// visible to every TS file — including the Worker — without import.
// This file intentionally empty; kept as an anchor in case we need
// per-module ambient declarations later.

export {};
